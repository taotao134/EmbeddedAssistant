# 嵌入式调试台使用说明

## 启动

只运行：

```text
ReleasePackages\嵌入式调试台-win-x64\DeviceDebugStudio.exe
```

程序为 Windows x64 自包含版本，不需要安装 .NET 运行库。

## 设备工作区

- 左侧选择或新建设备；连接参数和快捷命令会自动保存，也可点击“保存设备配置”立即保存并查看保存位置。
- 通信方式选择栏右侧的“+”可添加独立窗口；每个窗口拥有独立终端和连接会话，可同时打开多个串口、TCP、UDP 或 BLE 连接。
- “配置操作”可选择一个或多个 `sscom51.ini`、`netassist.cfg` 文件导入，不会修改原文件。
- “配置操作”还支持设备 JSON 导入导出、打开捕获 `.db`，以及修改设备配置保存目录。
- 默认配置目录为 `%LocalAppData%\DeviceDebugStudio\Profiles`，修改后会保存在本机设置中。

## 通信

- 串口支持自动热插拔刷新、任意正整数波特率、DTR 和 RTS。程序不会按驱动上报值限制波特率；打开操作在后台专用线程执行，超过 `500 ms` 未完成就取消并关闭端口后报错，避免界面未响应。
- 快捷指令栏的“AI导入”可选择任意嵌入式工程根目录，生成并复制一段不依赖固定盘符、工程名、源码文件名或参考 JSON 的 Codex 指令提取提示词。
- TCP 服务器向空目标发送时会广播给全部已连接客户端。
- UDP 支持单播、广播和组播；目标格式为地址与端口。
- 循环发送使用独立高精度调度线程，按相邻两次发包起点计算周期；发送内容在启动循环时预处理，界面刷新和诊断日志不会参与下一包定时。
- 经典蓝牙串口通过 Windows 配对后作为 COM 口使用。
- BLE GATT 扫描优先使用附近 BLE 广播；没有收到广播时才回退到 Windows 已配对/已关联的 BLE 设备，以避免扫描等待。Windows 显示“电脑”等类型只是 Appearance 外观分类，不等于经典蓝牙。
- BLE GATT 先扫描设备，再浏览服务与特征值；选择树节点可自动填入读、写或通知 UUID。读 UUID 仅在手动点击“读取”时需要，留空会自动选择支持 Read 的特征值；通知 UUID 用于设备主动上报。

## 数据与协议

- 终端支持 ASCII、UTF-8、GBK 和 HEX，清空界面不会删除捕获文件。
- 终端“表格”页用于按字段查看；“连续文本”页支持像文本编辑器一样跨行拖选，并可用 `Ctrl+C` 或右键复制。
- 通信捕获默认关闭；需要保存收发数据时，在终端工具栏开启“记录”，下一次连接会在 `%USERPROFILE%\Documents\DeviceDebugStudio\Captures` 创建 SQLite 捕获文件。
- 帧解析支持原始块、文本行、分隔符、固定长度、长度字段和空闲间隔。
- 帧字段、字节序、比例、位域、转义和校验在“帧解析”页的 JSON 模板中配置；模板框中间的竖线可左右拖动调整表格与模板区域。
- 帧模板支持单个 JSON 对象或 JSON 数组。数组模板共用上方分帧方式，按 `MatchOffset`、`MatchHex` 选择具体帧格式；`MatchHex` 留空表示兜底模板。一个字段包含多个内容时拆成多个 `Fields`，有长度前缀的变长内容使用 `LengthFromOffset`，其后的字段用 `OffsetFromLengthOffset` 自动后移。
- S06 示例模板和 TX/RX 回环数据位于 `tests\TestData\S06-frame-templates.json` 与 `tests\TestData\S06-loopback-frames.txt`，可直接在“帧解析”页导入模板，再按 HEX 逐帧或连续发送测试。
- Modbus 页支持常用 RTU/TCP 请求；启用从站模拟后，串口按 RTU 响应，TCP Server 按 Modbus TCP 响应。

## 快捷命令

- 命令可使用文本或 HEX，可选择换行、校验和循环间隔。
- 快捷指令行与快捷参数方案相互独立：指令行的 `Payload` 负责指令行发送和快捷指令循环发送；编辑器中的 `Template`、变量和方案负责方案预览、方案发送和快捷参数循环发送。选择方案或修改方案变量不会改写指令行，修改指令行也不会改写方案。
- 在 Payload 中输入带逗号参数的命令并按回车或离开输入框时，程序会自动把参数位置转换为 `${param1}`、`${param2}` 等模板变量，并将非空输入值填入当前变量方案；Payload 原文保持不变，空参数位置也会生成对应变量。已经手工编辑过的模板不会被覆盖。
- AI 导入生成的 JSON 中，带参数命令应同时保存完整默认 `Payload`、变量 `Template`、`VariableSets` 和有效的 `SelectedVariableSetId`；不能用方案预览值替代 `Payload`。
- 顶部搜索框按名称或发送内容定位命令，排序可选使用频率、最近使用或手动顺序。
- “命令变量（可选）”按 `key=value` 每行填写，在命令中使用 `${key}` 引用；例如填写 `id=01` 后，`${id}` 会替换为 `01`。
- 连接参数在会话建立后会锁定；断开后才能切换端口或网络端点。

## 在线更新

在“设置”窗口的“在线更新”区域填写 GitHub 仓库（例如 `owner/repository`），可选择启动时检查。发布版本的 GitHub Release 标签应为 `v1.2.3` 形式，并上传 `win-x64` ZIP 和对应的 `.sha256` 校验文件。程序会在后台检查新版本，下载后校验 SHA-256，再由安装代理覆盖程序文件并重启；设备配置和捕获文件保存在用户数据目录，不会被更新包覆盖。

## 日志与诊断

- 调试日志默认关闭；在“设置 > 诊断 > 日志与反馈”开启后，通用日志和串口工作进程日志写入 `%LocalAppData%\DeviceDebugStudio\Logs`。
- 在线更新安装失败日志始终自动归档到 `%LocalAppData%\DeviceDebugStudio\Updates`，不受调试日志开关影响。
- “导出诊断包”会打包上述日志；“GitHub 提交问题”只打开预填的问题页面，不会自动上传本地文件。
